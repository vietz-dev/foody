apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foody.fullname" . }}-test-connection"
  labels:
    {{- include "foody.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '{{ include "foody.fullname" . }}-web:{{ .Values.web.service.port }}']
  restartPolicy: Never
