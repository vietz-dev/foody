apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foody.fullname" . }}-test-connection"
  labels:
    {{- include "foody.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '{{ include "foody.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
